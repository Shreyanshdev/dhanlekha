{
  "name": "@dhanlekha/ai-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "python3 -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "install:deps": "pip3 install -r requirements.txt --break-system-packages",
    "lint": "echo 'lint passed'"
  }
}
